╔══════════════════════════════════════════╗
║                   ЖУРНАЛ ОТЧЁТА                          ║
║             (каждый ведёт свой — одинаковый формат)      ║
╚══════════════════════════════════════════╝


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[21.05.2026] — models.py (датаклассы)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Сделано:
  - описаны датаклассы EmailMessage, QueueItem, EmailStatus
  - поле to автоматически приводится к списку если передана строка
  - поля cc и bcc по умолчанию пустые списки

🐛 Найден баг:
  - если передать to как строку с несколькими адресами через запятую,
    например "a@x.com, b@x.com", она не разбивалась на два адреса,
    а добавлялась как один получатель целиком
  - письмо уходило только первому, второй его не получал
  - [статус: исправлен — добавлен split(",") с strip() при приведении строки к списку]

📌 Следующий шаг:
  - добавить валидацию формата email-адреса

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[21.05.2026] — logger.py (журналирование)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Сделано:
  - RotatingFileHandler: ротация при 5 МБ, хранится 3 копии
  - консоль выводит WARNING и выше, файл — всё начиная с DEBUG

🐛 Найден баг:
  - при первом запуске программа падала с FileNotFoundError:
    [Errno 2] No such file or directory: 'logs/app.log'
  - причина: директория logs/ не создавалась автоматически,
    а RotatingFileHandler не умеет создавать промежуточные папки
  - [статус: исправлен — добавлен вызов log_file.parent.mkdir(parents=True, exist_ok=True)
    перед созданием handler-а]

📌 Следующий шаг:
  - проверить ротацию на больших объёмах логов

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[22.05.2026] — config.py (загрузка конфигурации)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Сделано:
  - реализована загрузка настроек из config.ini через configparser
  - добавлена поддержка переменных окружения с приоритетом над файлом
  - дефолтные значения на случай отсутствия конфига

🐛 Найден баг:
  - при запуске на Windows программа падала с ошибкой:
    UnicodeDecodeError: 'charmap' codec can't decode byte 0x8d
  - причина: configparser читал файл в кодировке cp1252, а config.ini
    содержит кириллические комментарии в UTF-8
  - [статус: исправлен — добавлен параметр encoding="utf-8" в parser.read()]

🐛 Найден баг:
  - если в config.ini указать port = abc (нечисловое значение),
    программа падала с необъяснимым ValueError: invalid literal for int()
    без указания на то, в каком именно параметре ошибка
  - [статус: исправлен — добавлена обёртка try/except с понятным сообщением:
    "Некорректное значение 'port' в конфиге: ожидается целое число"]

📌 Следующий шаг:
  - протестировать загрузку при полностью отсутствующем config.ini

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[22.05.2026] — queue_manager.py (очередь отправки)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Сделано:
  - персистентная JSON-очередь с уникальными UUID для каждого письма
  - методы add(), get_pending(), mark_sent(), mark_failed(), stats()

🐛 Найден баг:
  - при первом запуске команды queue, если файл data/queue.json
    ещё не существовал, программа падала с FileNotFoundError
  - причина: QueueManager пытался открыть файл на чтение до его создания
  - [статус: исправлен — добавлена проверка: если файл не существует,
    инициализировать пустым списком и создать файл]

🐛 Найден баг:
  - метод mark_failed() каждый раз увеличивал счётчик retries даже если
    элемент уже имел статус FAILED (исчерпал все попытки)
  - при повторном вызове process счётчик уходил в бесконечность
  - [статус: исправлен — добавлена проверка: если статус уже FAILED, пропускать]

📌 Следующий шаг:
  - добавить команду для ручной очистки элементов со статусом failed

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[23.05.2026] — template_engine.py + base.html (шаблонизатор)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Сделано:
  - обёртка над Jinja2 с автоэкранированием HTML
  - базовый шаблон base.html с наследованием
  - шаблоны daily_report.html и weekly_report.html

🐛 Найден баг:
  - при рендере любого шаблона Jinja2 выбрасывал:
    TemplateAssertionError: block 'title' defined twice
  - причина: блок {% block title %} был объявлен в base.html дважды —
    в теге <title> и в теге <h1> шапки письма
  - [статус: исправлен — второй вызов заменён на {{ self.title() }}]

🐛 Найден баг:
  - если передать в --context строку с HTML-спецсимволами (например,
    recipient_name = "<Иван>"), Jinja2 экранировал их в &lt;Иван&gt;
    прямо в теме письма, хотя тема — обычный текст, не HTML
  - получатель видел "&lt;Иван&gt;" вместо "<Иван>"
  - [статус: исправлен — тема письма формируется до рендера шаблона,
    из сырых данных контекста, минуя Jinja2]

📌 Следующий шаг:
  - добавить шаблон для уведомлений об ошибках

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[23.05.2026] — mailer.py (SMTP-отправка)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Сделано:
  - поддержка STARTTLS (порт 587) и SSL (порт 465)
  - корректное формирование заголовков From, To, Cc, Subject

🐛 Найден баг:
  - когда список cc был пустым, в письмо всё равно добавлялся
    заголовок "Cc: " с пустым значением
  - некоторые почтовые клиенты (Outlook) воспринимали это как ошибку
    и помечали письмо как подозрительное
  - [статус: исправлен — заголовок Cc добавляется только если список непустой]

🐛 Найден баг:
  - при ошибке аутентификации (неверный пароль) программа выбрасывала
    SMTPAuthenticationError со стектрейсом, где в тексте ошибки
    в открытом виде фигурировал пароль из конфига
  - [статус: исправлен — перехват исключения с заменой сообщения на
    "Ошибка аутентификации SMTP: проверьте username и password в конфиге"]

📌 Следующий шаг:
  - добавить таймаут соединения, сейчас при недоступном SMTP-сервере
    программа зависает навсегда

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[24.05.2026] — main.py (CLI-точка входа)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Сделано:
  - команды send, queue, process, status через argparse
  - вывод статистики очереди в консоль

🐛 Найден баг:
  - команда process при пустой очереди завершалась молча —
    никакого вывода в консоль, пользователь не понимал,
    отработала ли программа или зависла
  - [статус: исправлен — добавлен вывод "Очередь пуста, нечего отправлять"
    если get_pending() вернул пустой список]

🐛 Найден баг:
  - если передать невалидный JSON в --context (например, пропустить
    закрывающую скобку), программа падала с трейсбеком json.JSONDecodeError
    вместо понятного сообщения об ошибке
  - [статус: исправлен — добавлен try/except с выводом:
    "Ошибка: --context содержит невалидный JSON. Проверьте синтаксис."]

📌 Следующий шаг:
  - добавить команду clear для удаления писем со статусом failed из очереди
