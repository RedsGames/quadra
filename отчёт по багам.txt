╔══════════════════════════════════════════════════════════╗
║                   ЖУРНАЛ ОТЧЁТА                          ║
║         (каждый ведёт свой — одинаковый формат)          ║
╚══════════════════════════════════════════════════════════╝


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[21.05.2026] — models.py (датакласс Transaction)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Сделано:
  - описан датакласс Transaction с полями id, type, category, amount, date, description
  - метод to_dict() через dataclasses.asdict() для сериализации в JSON
  - поле description имеет значение по умолчанию ""

🐛 Найден баг:
  - поле date принимает любую строку, в том числе "вчера" или "abc"
  - при дальнейшей фильтрации по дате такие записи могли попадать
    в результаты при поиске по подстроке (например, "вчера" содержит "е"
    — не падает, но ведёт себя непредсказуемо)
  - [статус: известен — валидация формата YYYY-MM-DD не реализована,
    пользователь должен самостоятельно вводить корректную дату]

📌 Следующий шаг:
  - добавить валидацию даты через datetime.strptime при добавлении транзакции

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[21.05.2026] — storage.py (JSON-хранилище)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Сделано:
  - JsonStorage.load() возвращает пустой список если файл не существует
  - JsonStorage.save() пишет с ensure_ascii=False и indent=4

🐛 Найден баг:
  - метод load() перехватывал как JSONDecodeError, так и IOError,
    и в обоих случаях молча возвращал пустой список []
  - если файл существовал, но был повреждён из-за сбоя питания,
    программа стартовала как будто данных нет совсем,
    а старые транзакции были безвозвратно потеряны без предупреждения
  - [статус: известен — логирование ошибки не реализовано,
    пользователь не получает уведомления о повреждённом файле]

📌 Следующий шаг:
  - добавить print-предупреждение или logging при ошибке чтения файла

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[22.05.2026] — services.py (бизнес-логика)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Сделано:
  - add_transaction() автоматически генерирует id через max(..., default=0) + 1
  - get_balance() считает доходы и расходы раздельно через sum() и вычитает
  - filter_transactions() поддерживает поиск по подстроке даты и по категории

🐛 Найден баг:
  - при первом вызове add_transaction() на пустом списке транзакций
    вызов max([t.id for t in self.transactions]) падал с ValueError:
    max() arg is an empty sequence
  - программа аварийно завершалась при попытке добавить первую запись
  - [статус: исправлен — добавлен аргумент default=0 в вызов max()]

🐛 Найден баг:
  - метод get_balance() при работе с суммами, имеющими много знаков после запятой
    (например, 100.1 + 200.2), возвращал 300.30000000000003 вместо 300.30
  - это объясняется стандартной неточностью float IEEE 754
  - [статус: известен — округление до 2 знаков не добавлено,
    вывод в консоли не всегда выглядит корректно]

📌 Следующий шаг:
  - использовать round(balance, 2) при выводе или decimal.Decimal для расчётов

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[22.05.2026] — cli.py (консольный интерфейс) — часть 1
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Сделано:
  - строгий выбор типа операции через цифры 1 (доход) / 2 (расход)
  - метод _show() выводит транзакции в виде таблицы с фиксированными колонками

🐛 Найден баг:
  - в методе _add() ввод типа операции (type_input) обрабатывался так:
    t_type = "income" if type_input == "1" else "expense"
  - если пользователь вводил что угодно кроме "1" (например, "доход" или "3"),
    тип автоматически становился "expense" без предупреждения
  - баланс считался неверно: все нераспознанные типы уходили в расходы
  - [статус: известен — явная валидация ввода не добавлена]

🐛 Найден баг:
  - фильтрация по категории не работала, если пользователь вводил
    "  Зарплата  " (с пробелами по краям) или "ЗАРПЛАТА" (в верхнем регистре),
    хотя в базе категория хранилась как "зарплата" (строчными, без пробелов)
  - [статус: исправлен — добавлены .strip() и .lower() к вводу пользователя
    и к stored category при сравнении]

📌 Следующий шаг:
  - добавить явную проверку: если type_input не "1" и не "2" — вывести ошибку и повторить ввод

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[23.05.2026] — cli.py (консольный интерфейс) — часть 2
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Сделано:
  - метод _show() выводит разделитель под заголовком таблицы

🐛 Найден баг:
  - разделительная линия таблицы строилась как "-" * len(header) * 2
  - len(header) — длина строки заголовка (~65 символов), умноженная на 2
    давала строку из 130 дефисов, что вдвое длиннее самой таблицы
  - на узких терминалах строка переносилась и ломала визуальное выравнивание
  - [статус: исправлен — убран * 2, длина разделителя теперь равна len(header)]

📌 Следующий шаг:
  - протестировать вывод на терминале шириной 80 символов

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[23.05.2026] — services.py (export_summary)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Сделано:
  - export_summary() пишет отчёт с кодировкой utf-8-sig для Windows Блокнота
  - в таблицу добавлены колонки ID и описание

🐛 Найден баг:
  - в первой версии export_summary в таблице отсутствовали колонки ID и описание —
    отчёт содержал только дату, тип, категорию и сумму
  - при разборе отчёта вручную было невозможно сопоставить строку с записью в программе
  - [статус: исправлен — структура таблицы расширена, добавлены ID и description]

🐛 Найден баг:
  - в тесте test_export_existence.py файл читался с кодировкой 'utf-8',
    а записывался с 'utf-8-sig' (с BOM-маркером)
  - при чтении контента в тесте первый символ отчёта содержал BOM (﻿),
    что приводило к ложным ошибкам при поиске по содержимому в Windows
  - [статус: известен — тест проходит, но BOM может мешать при ручном парсинге]

📌 Следующий шаг:
  - открывать файл в тесте с encoding='utf-8-sig' для корректного пропуска BOM

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[24.05.2026] — visualization.py (круговая диаграмма)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Сделано:
  - ChartGenerator.generate_expense_pie_chart() группирует расходы по категориям
  - при отсутствии расходов метод завершается без ошибки

🐛 Найден баг:
  - при вызове generate_expense_pie_chart() с output_path = "chart.png"
    (без директории в пути) выполнялось os.makedirs(os.path.dirname("chart.png"))
  - os.path.dirname("chart.png") возвращает пустую строку ""
  - os.makedirs("") поднимал FileNotFoundError: [Errno 2] No such file or directory: ''
  - диаграмма не сохранялась, программа падала
  - [статус: исправлен — добавлена проверка: если dirname пустой, makedirs не вызывается]

📌 Следующий шаг:
  - добавить параметр по умолчанию output_path = "data/chart.png"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[24.05.2026] — main.py (точка входа)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Сделано:
  - main() создаёт папку data/ если она не существует
  - filename принудительно перемещается в data/ через os.path.join

🐛 Найден баг:
  - пользователь мог ввести имя файла с обходом директории: "../../secret.json"
  - os.path.join("data", "../../secret.json") = "data/../../secret.json"
    что раскрывалось в путь выше корня проекта
  - файл базы данных создавался за пределами папки data/
  - [статус: известен — проверка допустимости пути не реализована]

📌 Следующий шаг:
  - проверять, что итоговый путь остаётся внутри папки data/
    через os.path.abspath и сравнение с базовой директорией
