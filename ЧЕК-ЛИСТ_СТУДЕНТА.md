╔═══════════════════════════════════════════════════════════╗
║           ЗАДАЧИ ПО МОДУЛЯМ — ИНДИВИДУАЛЬНО               ║
║  👾 - в работе   ✅ - сделано  ❔- нужно реализовать    ║
╚═══════════════════════════════════════════════════════════╝

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[Трекер доходов и расходов] — Модуль 1
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[✅] — датакласс Transaction (id, type, category, amount, date, description)
[✅] — хранилище JsonStorage: load() и save() через json-файл
[✅] — сервис FinanceService: добавление транзакции с автоинкрементом id
[✅] — сервис FinanceService: удаление транзакции по id
[✅] — расчёт текущего баланса (доходы − расходы)
[✅] — фильтрация транзакций по дате (подстрока) и по категории (без учёта регистра)
[✅] — данные для Chart.js: расходы по категориям
[✅] — экспорт списка транзакций в TXT-файл (отформатированная таблица)
[✅] — FastAPI роутер: GET /tracker — страница со списком
[✅] — FastAPI роутер: POST /tracker/add — добавить транзакцию через форму
[✅] — FastAPI роутер: POST /tracker/delete/{id} — удалить транзакцию
[✅] — FastAPI роутер: GET /tracker/export — скачать TXT-отчёт
[✅] — HTML-шаблон tracker.html с таблицей и формой добавления
[✅] — валидация: сумма должна быть > 0, иначе HTTP 422

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[QR-генератор] — Модуль 2
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[✅] — create_qr_image(data, caption, box_size): QR-код через qrcode + Pillow
[✅] — подпись (caption) рисуется под QR через ImageDraw и ImageFont
[✅] — qr_to_bytes(): возвращает PNG-байты для отдачи через HTTP
[✅] — batch_qr_zip(lines): ZIP-архив с QR-кодами для каждой строки из списка
[✅] — пустые строки в пакете пропускаются без ошибки
[✅] — настройка размера ячейки QR через параметр box_size
[✅] — FastAPI роутер: GET /qr — страница генератора
[✅] — FastAPI роутер: POST /qr/generate — вернуть PNG одиночного QR
[✅] — FastAPI роутер: POST /qr/batch — принять TXT-файл, вернуть ZIP
[✅] — HTML-шаблон qr.html с предпросмотром сгенерированного QR
[✅] — убрана зависимость от tkinter (только qrcode и Pillow)
[✅] — исправлен баг: box_size теперь передаётся во все функции явно

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[Калькулятор бюджета поездки] — Модуль 3
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[✅] — датакласс TripProfile (id, name, destination, currency, budget_items, total_base, total_converted)
[✅] — BudgetService.create_profile(): создание поездки с расчётом total_base
[✅] — уникальный id поездки генерируется через uuid4 (первые 8 символов)
[✅] — BudgetService.update_converted(): сохраняет итог после конвертации
[✅] — BudgetService.get_profile() и list_profiles(): чтение профилей
[✅] — BudgetService.delete_profile(): удаление поездки
[✅] — конвертация валют через внешний API frankfurter.app (httpx, timeout=5)
[✅] — если API конвертации недоступен — rate=1.0 (нет краша приложения)
[✅] — профили сохраняются в data/budgets.json (персистентность)
[✅] — FastAPI роутер: GET /budget — страница списка поездок
[✅] — FastAPI роутер: POST /budget/calculate — рассчитать и сохранить поездку
[✅] — FastAPI роутер: GET /budget/convert — JSON-API конвертации валют
[✅] — FastAPI роутер: GET /budget/{id} — загрузить сохранённую поездку
[✅] — FastAPI роутер: POST /budget/delete/{id} — удалить поездку
[✅] — HTML-шаблон budget.html с формой и результатом расчёта

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[Email-отправщик] — Модуль 4
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[✅] — Mailer: отправка через smtplib.SMTP_SSL (порт 465)
[✅] — Mailer: формирование MIME-письма с HTML-телом
[✅] — настройки SMTP читаются из переменных окружения (.env)
[✅] — EmailQueueManager.send_now(): немедленная отправка с записью в журнал
[✅] — EmailQueueManager.schedule(): отложенная отправка через APScheduler
[✅] — журнал отправленных/запланированных писем хранится в памяти
[✅] — статус каждого задания: "sent" / "scheduled" / "error"
[✅] — FastAPI роутер: GET /email — страница с формой и историей
[✅] — FastAPI роутер: POST /email/send — отправить письмо
[✅] — FastAPI роутер: POST /email/schedule — запланировать отправку
[✅] — FastAPI роутер: GET /email/status — статус очереди (JSON)
[✅] — HTML-шаблон email.html с формой, историей и предупреждением если SMTP не настроен
[✅] — если .env не заполнен — модуль email отключается, остальное работает

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[Dashboard — главная страница]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[✅] — виджет текущего баланса (из трекера)
[✅] — последние 5 транзакций
[✅] — последние 3 сохранённые поездки
[✅] — статус очереди email (последние 5 задач)
[✅] — ссылки-быстрые действия на все четыре модуля
[✅] — анимированные счётчики в stat-карточках
[✅] — адаптивная вёрстка (Tailwind CSS)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[Тесты pytest]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[✅] — tests/test_tracker.py — 14 тест-кейсов (Transaction, FinanceService, JsonStorage)
[✅] — tests/test_qr.py — 9 тест-кейсов (create_qr_image, qr_to_bytes, batch_qr_zip)
[✅] — tests/test_budget.py — 11 тест-кейсов (BudgetService, TripProfile, персистентность)
[✅] — tests/test_mailer.py — 4 тест-кейса (Mailer, SMTP мокируется через unittest.mock)
[✅] — покрытие ключевых модулей ≥ 89%
[✅] — тесты не используют реальный SMTP, реальный диск (tmp_path), реальную сеть

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[Инфраструктура и деплой]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[✅] — requirements.txt с зафиксированными версиями зависимостей
[✅] — .env для конфигурации без хардкода
[✅] — статика подключена через StaticFiles (/static)
[✅] — Jinja2Templates настроен в main.py
[✅] — развёртывание на сервере redsgames.ru
[✅] — Nginx проксирует запросы на uvicorn (порт 8000)
[✅] — SSL через Let's Encrypt (certbot)
[✅] — systemd-сервис для автозапуска приложения

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ХРОНОЛОГИЯ СОЗДАНИЯ МОДУЛЕЙ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

20.05.2026 (ср) — Подготовка и трекер
  [✅] Transaction (models.py)    — датакласс с to_dict()
  [✅] JsonStorage (storage.py)   — load() / save() в JSON
  [✅] FinanceService (services.py) — вся бизнес-логика трекера

21.05.2026 (чт) — Роутер трекера и QR-модуль
  [✅] tracker_routes.py          — FastAPI-роутер трекера
  [✅] tracker.html               — шаблон с таблицей и диаграммой
  [✅] qr_utils.py                — create_qr_image, qr_to_bytes, batch_qr_zip
  [✅] qr_routes.py               — FastAPI-роутер QR

22.05.2026 (пт) — Калькулятор бюджета
  [✅] BudgetService (service.py) — профили поездок, конвертация
  [✅] TripProfile (dataclass)    — модель поездки
  [✅] budget_routes.py           — FastAPI-роутер бюджета с /convert
  [✅] budget.html                — шаблон с формой и результатом

23.05.2026 (сб) — Email и планировщик
  [✅] Mailer (mailer.py)         — SMTP_SSL отправка через smtplib
  [✅] EmailQueueManager          — очередь + APScheduler
  [✅] email_routes.py            — FastAPI-роутер email
  [✅] email.html                 — шаблон с формой и историей

24.05.2026 (вс) — Dashboard и интеграция
  [✅] main.py                    — подключение всех роутеров
  [✅] dashboard.html             — сводная страница всех модулей
  [✅] base.html                  — базовый шаблон (навигация, Tailwind)

25.05.2026 (пн) — Тесты, баги и документация
  [✅] tests/test_tracker.py
  [✅] tests/test_qr.py
  [✅] tests/test_budget.py
  [✅] tests/test_mailer.py
  [✅] README.md                  — полная документация проекта
  [✅] Исправление бага box_size в QR-модуле
  [✅] Исправление бага типа в filter_transactions
  [✅] Деплой на redsgames.ru
