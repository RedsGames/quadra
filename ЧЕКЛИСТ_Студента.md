╔═══════════════════════════════════════════════════════════╗
║           ЗАДАЧИ ПО МОДУЛЯМ — ИНДИВИДУАЛЬНО               ║
║  👾 - в работе   ✅ - сделано  ❔- нужно реализовать    ║
╚═══════════════════════════════════════════════════════════╝

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[Личный финансовый трекер] — Женя
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[✅] — добавление транзакции (тип доход/расход, категория, дата, описание)
[✅] — список пресет-категорий (зарплата, продукты, транспорт, развлечения)
[✅] — просмотр всех транзакций в виде таблицы с заголовками
[✅] — фильтрация по году / месяцу / полной дате
[✅] — фильтрация по категории без учёта регистра и пробелов
[✅] — подсчёт итогового баланса (доходы − расходы)
[✅] — сохранение транзакций в JSON-файл (data/*.json)
[✅] — загрузка транзакций из JSON-файла при запуске
[✅] — удаление транзакции по ID
[✅] — экспорт финансового отчёта в .txt с выравниванием столбцов
[✅] — консольный интерфейс с меню и паузой перед возвратом
[✅] — альтернативное SQLite-хранилище (database.py)
[✅] — интерфейсы-протоколы для хранилища и визуализатора (protocols.py)
[✅] — визуализация расходов (круговая диаграмма, visualization.py)
[✅] — тестирование: расчёт баланса, фильтрация, экспорт (pytest)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ХРОНОЛОГИЯ СОЗДАНИЯ МОДУЛЕЙ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

21.05.2026 (чт) — Старт проекта. Модели и хранилище
  [✅] models.py          — датакласс Transaction (id, type, category, amount, date, description)
  [✅] storage.py         — JsonStorage: load() и save() через json + utf-8

22.05.2026 (пт) — Бизнес-логика и архитектура
  [✅] services.py        — FinanceService: add, delete, balance, filter, export
  [✅] protocols.py       — протоколы ITransactionRepository и IVisualizer

23.05.2026 (сб) — Интерфейс и альтернативный бэкенд
  [✅] cli.py             — ConsoleUI: меню, _add, _show, _filter, _delete, _export
  [✅] database.py        — SQLiteRepository: add, get_all, get_by_period, delete_by_id

24.05.2026 (вс) — Визуализация, точка входа и тесты
  [✅] visualization.py   — ChartGenerator: круговая диаграмма расходов по категориям
  [✅] main.py            — точка входа: инициализация data/, выбор файла БД
  [✅] tests/test_scripts.py  — тест load_config и нормального завершения main()
  [✅] tests/test_report.py   — тест баланса, фильтрации (год/месяц/категория), экспорта

25.05.2026 (пн) — Отладка и документация
  [✅] README.md              — документация проекта с примерами и архитектурой
  [✅] ЧЕК-ЛИСТ_СТУДЕНТА.md  — журнал задач по модулям
  [✅] Исправление бага в services.py (max() на пустом списке → default=0)
  [✅] Исправление бага в cli.py (фильтрация с лишними пробелами → .strip())
  [✅] Исправление бага в storage.py (кодировка UTF-8 при сохранении)
  [✅] Исправление бага в cli.py (поле type принимало произвольный текст → строгий выбор 1/2)
  [✅] Исправление бага в services.py (export_summary — добавлены колонки ID и описание)
